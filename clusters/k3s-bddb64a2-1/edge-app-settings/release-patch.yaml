apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: edge-app-settings
  namespace: cluster-config
spec:
  chart:
    spec:
      chart: edge-app-settings
      sourceRef:
        kind: HelmRepository
        name: edge-app-settings
        namespace: cluster-config
  values:
    configMap:
      name: edge-app-configmap
      data:
        TEMPERATURE: "20"
        PRESSURE: "30"
        VELOCITY: "40"
    secret:
      name: edge-app-secret
      stringData:
        USERNAME: "arcuser"
        PASSWORD: "95812f285e7890569bf1"
        TOKEN: "OTU4MTJmMjg1ZTc4OTA1NjliZjEK"
